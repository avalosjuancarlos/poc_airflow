# Apache Airflow Requirements
# This file contains additional Python packages for your Airflow environment

# Packages for market data feature
requests==2.32.5
pandas==2.1.4
pyarrow==14.0.1  # For Parquet file format

# Data Warehouse dependencies
sqlalchemy==1.4.54
psycopg2-binary==2.9.9  # PostgreSQL driver (dev environment)
redshift-connector==2.0.918  # Redshift driver (staging/production)

# Testing dependencies
pytest==7.4.3
pytest-cov==4.1.0
pytest-mock==3.12.0
requests-mock==1.11.0
freezegun==1.4.0

# Optional: Monitoring and Error Tracking
# Note: These are not integrated by default. To add them:
# 1. Uncomment the package below
# 2. Extend dags/market_data/utils/logger.py (see docs/user-guide/logging.md)
# 3. Configure environment variables in .env

# Sentry integration for error tracking and monitoring
# sentry-sdk==1.40.0

# Datadog APM integration for metrics and tracing
# ddtrace==2.3.0

# Uncomment and add packages as needed for your DAGs
# boto3==1.28.0
# sqlalchemy==2.0.0
# psycopg2-binary==2.9.7

